# Environment Variables for Hostinger Deployment
# Save as: .env.production in public_html/ OR set in Hostinger control panel

# ============================================
# SUPABASE CONFIGURATION (REQUIRED)
# ============================================
# Get these from: Supabase Project Settings → API

VITE_SUPABASE_URL=https://vkvreculoijplklylpsz.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# STRIPE CONFIGURATION (REQUIRED for payments)
# ============================================
# Get from: Stripe Dashboard → Developers → API Keys

VITE_STRIPE_PUBLIC_KEY=pk_test_51234...  # Test key - REPLACE with live key for production
# VITE_STRIPE_PUBLIC_KEY=pk_live_51234... # Live key - use this in production

# ============================================
# OPTIONAL: Additional Configuration
# ============================================

# None needed for core functionality

# ============================================
# IMPORTANT NOTES
# ============================================

# 1. VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are CRITICAL
#    Without these, the app will show a blank page
#    The AuthContext cannot initialize Supabase client
#
# 2. VITE_STRIPE_PUBLIC_KEY is needed for payment features
#    If not set, payment modal will fail to load
#    Use pk_test_* for testing, pk_live_* for production
#
# 3. These variables are injected at BUILD TIME (npm run build)
#    They are NOT read at runtime from .env.production
#    For Hostinger deployment:
#    - Option A: Set in Hostinger Environment Variables (preferred)
#    - Option B: Include in .env.production in public_html/
#
# 4. NEVER commit this file with real keys to Git
#    Add .env.production to .gitignore
#
# 5. Keep keys secret. If compromised, regenerate from Supabase/Stripe

# ============================================
# HOW TO GET YOUR KEYS
# ============================================

# SUPABASE_URL:
# 1. Log in to Supabase Dashboard
# 2. Select your project
# 3. Settings → API
# 4. Copy "Project URL"

# SUPABASE_ANON_KEY:
# 1. Same page (Settings → API)
# 2. Copy "anon public" key (NOT service_role)
# 3. Key starts with "eyJ..."

# STRIPE_PUBLIC_KEY:
# 1. Log in to Stripe Dashboard
# 2. Developers → API Keys
# 3. Copy "Publishable key"
# 4. Starts with "pk_test_" or "pk_live_"

# ============================================
# DEPLOYMENT METHOD
# ============================================

# If using .env.production:
# 1. Create file .env.production in public_html/
# 2. Set variables below with YOUR actual keys
# 3. This file is NOT included in dist/ build
# 4. You must manually set these on Hostinger

# Better: Use Hostinger control panel:
# 1. Dashboard → Settings → Environment Variables
# 2. Add each variable there
# 3. Server reads them automatically
# 4. No need to commit to files
